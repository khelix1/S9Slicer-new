# INSTALL

## Dependencies

- CMake
- C++17 Compiler (e.g., g++, clang++)
- Clipper2 (now embedded)
- Qt6 (future GUI only)

## Build Instructions

```bash
mkdir build && cd build
cmake ..
make
```

Run with:

```bash
./s9slicer model.stl
```
